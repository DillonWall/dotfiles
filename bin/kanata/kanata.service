[Unit]
Description=Kanata Service
Requires=local-fs.target
After=local-fs.target

[Service]
Type=simple
ExecStart=/usr/bin/kanata -c /etc/kanata/my-config.kbd
Restart=no

[Install]
WantedBy=sysinit.target

